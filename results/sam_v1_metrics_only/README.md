# SAM V1 说明

`SAM V1` 在最终报告中作为失败对照保留，其指标如下：

- `Dice = 0.6534`
- `VF = 86.02%`

当前工作区只保留了该方法的最终统计结论，没有找到可直接纳入仓库的独立 `predictions/` 目录，因此这里仅保留说明，不附 10 张展示样例。

这一结果用于说明：

- 未进行合适归一化和流程修正时，模型会出现明显过分割
- `SAM LoRA V2` 的改进并非微小修补，而是完整流程优化后的结果
